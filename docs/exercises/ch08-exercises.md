# 第8章 练习

## 证明

1. 从 3-SAT 归约到顶点覆盖：画出变量器件和子句器件，证明 φ 可满足 ↔ G 有 ≤k 顶点覆盖
2. 证明 2-近似顶点覆盖和 ln(n)-近似集合覆盖的近似比
3. 追踪归约链 SAT → 3-SAT → 顶点覆盖 → 独立集 → 团 → TSP

## 建模

4. 归约是双向桥梁——算法方向和难性方向：
   - 算法方向：B 有算法 → A 也有（用 B 的算法解 A）
   - 难性方向：A 有下界 → B 有更紧的下界
   
   四核心源问题选择：3-SAT（逻辑问题）、整数划分（数值问题）、顶点覆盖（图选择问题）、哈密顿路径（图排序问题）。为什么是这四个？

5. 验证比发现容易 ↔ Agent 输出验证架构：
   - 自我一致性解码：多次采样取多数
   - 链式思维验证：检查推理步骤
   - 辩论框架：多个 Agent 论证取最优
   
   近似算法的"储蓄账户+股票市场"比喻如何映射到 Agent 输出管道设计？

## 实现

6. 实现极大匹配的 2-近似顶点覆盖算法，在随机图上验证近似比

---

<details>
<summary>参考答案提示</summary>

**1.1** 变量器件：每个变量 x_i 一条边 (v_{x_i}, v_{¬x_i})。子句器件：每个子句 3 个顶点的三角形。边连接变量顶点与子句顶点。k = |变量| + 2|子句|。

**2.1** 极大匹配 M 的每条边至少有一个端点在最优覆盖 OPT 中 → |OPT| ≥ |M| → 2|M| ≥ |OPT|。

**2.2** 贪心每步选覆盖最多未覆盖元素的集合。H(max|S_i|) ≤ ln(n)+1。

</details>